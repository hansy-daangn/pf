# 맥락 (CONTEXT) — 작업 전 반드시 먼저 읽기, 작업 후 반드시 갱신

> 규칙: 어떤 수정·작업이든 **이 파일을 먼저 확인**하고, 끝나면 **여기에 반영**한다. (모든 세션·모든 컴퓨터에서)

## 프로젝트
- 김한결(HANSY) 영상 디자이너 포트폴리오. repo `hansy-daangn/pf` → https://hansy-daangn.github.io/pf/
- 순수 정적: `index.html`(HTML/CSS/JS 전부 인라인) + `content.json`(편집 오버레이) + `assets/`
- `assets/video/`=카드 프리뷰(무음·저용량 루프), `assets/full/`=라이트박스용(고화질+소리), `assets/poster/`=썸네일, `assets/img/profile.jpg`=증명사진

## 절대 되돌리지 말 것 (이미 결정·완료된 사항)
- **극도 미니멀·화이트·세련.** 모든 요소는 이유가 있어야 함. 장식·라벨 군더더기 금지.
- **삭제한 군더더기(다시 만들지 말 것):** 영문 eyebrow(Résumé/Selected/Archive/Get in touch), 영문 소제목(Career/Education/Toolset), "Awards · 12", "스크롤하면 자동 재생됩니다", 소셜링크(Behance/Instagram/YouTube/Vimeo), 푸터 ©문구, 헤더 "HANSY · MOTION/VIDEO", 이름 옆 "HANSY".
- **이력서:** 내용은 전부 유지하되 면적 최소화. 구분선 2개만. 한글은 `word-break:keep-all`(어절 중간 줄바꿈 금지).
  - 수상: 검정색 통일·작은 폰트·가로 **3단**.
  - 기술: 소제목(2D/3D/AI/etc) 없이 칩만 나열.
  - 경력: **당근(Karrot) 1개만** (유아트/육사 등 삭제).
- **영상 24개.** 베스트3 = `td_review2605, mg_bizreview, td_declutter` = **정사각 1:1**. 나머지 카드 = 네이티브 비율(세로 9:16 / 가로 16:9).
- **라이트박스(클릭 재생):** `assets/full/<id>.mp4` 사용 = **고화질 + 소리(muted=false)** + **솔리드 흰배경** + 열릴 때 **배경 카드영상 전부 정지**. ※ 화질 나쁘게/음소거되면 안 됨 — 최우선·비협상.
- **제외:** B급 영상 전부 제외. 모션템플릿은 선택 케이스만.
- **삭제 유지:** 오펀 영상/포스터, `proposals.html`·`resume.html`·`시안.html` — 다시 만들지 말 것.
- **편집 기능:** 연필(✎) 버튼 → 모든 글자 인라인 수정 → 다시 누르면 `content.json`에 저장(브라우저에 1회 입력하는 GitHub Fine-grained PAT 사용, 토큰은 사용자 브라우저에만). **`data-k` 키·구조 유지해야 사용자 편집이 보존됨.**
- **사진:** `assets/img/profile.jpg`, 편집모드에서 드래그=위치·스크롤=확대, 값은 `content.json`의 `photo.bs/bx/by`.

## 배포 방법 (중요)
- Claude가 git 자격증명이 없어 **GitHub 웹UI로 직접 업로드+커밋**(파일 업로드 페이지 → Commit). 사용자가 직접 못 올리니 Claude가 푸시.
- **큰 커밋(>~15MB)은 "Processing…"에서 멈춤.** → **작은 커밋(~9MB 이하)으로 나눠** 올리고, 각 커밋이 실제 반영됐는지 **git tree로 확인 후** 다음 진행. 커밋 직후 페이지를 떠나면 유실되므로 완료 확인 필수.
- Pages 배포 ~30~60초. CDN 캐시 때문에 `?v=` 쿼리로 확인.

## 작업물 위치
- 로컬: `C:\Users\lalig\Desktop\ClaudeArea\영상디자이너포트폴리오_202606\`
- 사용자 선호: 결과물은 `{작업요약}_{YYYYMM}` 폴더 규칙.

## 최근 상태 (2026-06)
- 24개 작업(베스트3 정사각 + 신규 8: ai_closet2, ai_sea, 3d_sew, td_pickup, mg_jobs_spring, mg_rent2, mg_charge, mg_lyric) 반영.
- `assets/full/` 24개(720p·crf25·AAC) 업로드 완료. 오펀 자산 전부 삭제. index.html 극도 미니멀 리팩토링 배포 완료.

## 최근 상태 (2026-07)
- **작업 그룹핑 도입.** `W[i].variants=[{id,label}]`로 같은 캠페인 A/B/사이즈 변주를 하나의 카드로 묶음. 아카이브·베스트에는 primary만 노출, 카드 우상단 `+N` 뱃지, 라이트박스 하단 썸네일로 스위치. 현재 그룹: `mg_rent1[+mg_rent2]`, `ai_closet1[+ai_closet2]` — mg_rent2·ai_closet2는 standalone 카드에서 제외 (총 22 카드).
- **라이트박스 상세 KV.** 우측 정보 패널에 h3/role·year/desc 아래 KV 그리드(Campaign / Year / Category / 소재 수). 클릭·재생 시 `assets/full/<id>.mp4` + `muted=false` + `#main` display:none + 배경 카드 전부 pause 유지.
- **자기소개 라인.** `.rh-tag`/`.rh-desc` `max-width:30rem`로 통일해 어절 단위 자연 개행. `word-break:keep-all`+`overflow-wrap:break-word` 유지.
- **경력 1건만.** 빈 `r.car.i` 키는 content.json에서 삭제. 빈 org+det 행은 렌더 후 `display:none`으로 자동 숨김(안전망).
- **기술 CSV 편집.** 스킬 UI를 한 줄 flat 칩(카테고리 소제목 없음)으로 유지하되, 내부 저장을 단일 CSV 문자열 `r.sk`로 변경. ✎ 진입 시 칩 → 콤마 텍스트박스, ✎ 이탈 시 콤마 → 칩. 예전 `r.sk.i.j` 계층 키 폐기 완료.
- **로컬스토리지 폴백 제거.** `pf_edits`는 로드 시 즉시 clear. 저장은 GitHub PAT 성공 시에만 EDITS 갱신 — 새로고침 시 미저장 편집은 소멸(설계 오류 수정).
- **캐시 무효화.** `<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">` 3종 헤더 유지.
- **미리보기 자원 관리.** 카드는 `PREVIEW_RANGE=[0,8]`초 구간 반복. 라이트박스 열림·편집 진입 시 pauseAllPreviews, 종료 시 resumeVisiblePreviews. `#main` display:none으로 GPU/paint 부담 완전 차단.
- **자기소개 폭.** `.rh-tag`/`.rh-desc`의 `max-width` 완전 제거 → `.rh-name`처럼 1fr 컬럼 풀폭 사용(사용자 명시 요구). 다시 좁히지 말 것.
- **키워드 시즌 그룹 복구.** `mg_keyword_newyr`(신년·keywordNewyear·2601)·`mg_keyword_fall`(가을)을 git 히스토리에서 복구해 `assets/{video,full,poster}/`에 복원, `mg_keyword_spring`(봄, primary)의 variants로 묶음. plabel="봄". ⚠️ 위 "삭제 유지" 규칙의 예외 — 사용자 명시 요구로 복구했으니 **다시 삭제하지 말 것**. 복구본은 오디오 없는 프리뷰 원본이라 라이트박스 재생 시 무음(원본 hi-q 확보 시 assets/full/ 교체).
- **CreativeReporter 연동 완료.** 비밀번호 `0715`로 `newoydegfbnnqujgiips.supabase.co` RPC(`cr_load`, publishable key는 CreativeReporter repo에 공개) 접근. 소재별 실집행 성과 + Moloco CDN 멀티사이즈 프리뷰 URL을 뽑아 `index.html`의 `const PKG={...}`(약 7KB)로 인라인 임베드.
  - **라이트박스 = 패키지 뷰.** 좌측 미디어 스테이지(`#lbv` 비디오 / `#lbi` 이미지 토글) + 썸네일 갤러리(자체 세로영상 + Moloco 다사이즈 영상/이미지). 우측에 `#lbPerf` 성과 타일(광고비·노출·CTR·활성 유저·eCPA·완주율, null 자동 생략).
  - **Moloco CDN(`cdn-f.adsmoloco.com`) 직접 핫링크.** `access-control-allow-origin:*`·206 지원 → GitHub Pages에서 바로 재생. 이미지 썸네일은 애드블록 대비 `wsrv.nl` 프록시 경유, 메인 이미지는 direct + onerror시 wsrv 폴백.
  - **PKG 매핑된 11개 작업:** mg_keyword_spring(keyWord_Template 5영상), mg_rent1(realty), td_nintendo(jobs_newyearBogum), mg_jobs_spring, sf_10000walk(manboki), td_godaangn(goDaangn.V2), td_raffle_v, td_pickup, td_declutter·mg_simbatdan·ai_closet1(성과만). 데이터 재생성 스크립트는 스크래치패드 방식(cr_load.json → 그룹핑 → PKG).
  - **지표 정제.** MAU 캠페인이라 install 기반 eCPI는 오해소지로 제외. eCPA(활성 유저당)·CTR·VCR 채택. 저표본(노출<20만) CTR은 null 처리(예: jobs_spring 36% 아웃라이어 숨김).
  - **성과 갱신 필요 시:** 비번 0715로 cr_load 다시 받아 PKG 재생성. 새 지표(ROAS 등) 원하면 Google 채널 rows(`_g_convValue`) 활용.
- **라이트박스 닫기 스크롤 애니메이션 제거.** closeLB에서 `html.scrollBehavior='auto'`로 임시 전환 후 `scrollTo(0,lbScrollY)` → 맨 위에서부터 스무스 스크롤되던 어지러운 애니메이션 제거, 원위치로 즉시 복귀.
- **라이트박스 갤러리 영상/이미지 그룹 간격.** buildGallery가 영상 먼저·이미지 나중으로 정렬, renderGallery가 영상→이미지 경계에 `.thgap`(16px) 삽입. (요청: A B [간격] C D E)
- **편집 모드 소재 큐레이션(그룹 추가·소재 선택/해제).** 전체 CreativeReporter 카탈로그 = `creatives.json`(377 패키지, 약 180KB, **편집 진입 시에만 lazy fetch**). 편집 모드에서:
  - 카드 클릭 → **소재 관리 모달**(`#mg`). 현재 소재를 타일로 표시, 클릭 토글로 숨김/표시. 하단에 카탈로그 검색+추가.
  - `＋ 새 그룹`(ngFab, 편집 모드만) → 카탈로그에서 패키지 선택 → 새 아카이브 카드 생성(userWork). 생성 후 관리 모달로 전환, 카테고리 칩(모션/3D/AI)·삭제 제공.
  - **저장 모델(전부 content.json/EDITS):** `works.add`=JSON 배열(사용자 생성 그룹: id·cat·title·campaign·assets·perf·pv), `w.<id>.off`=숨긴 소재 key 배열, `w.<id>.xadd`=추가한 CDN 소재 배열. saveEdits의 `NEXT=assign({},EDITS,E)`로 자동 병합·PAT 저장.
  - **렌더:** `archiveWorks()=W.concat(userWorks())`, `cardHTML(w)`가 w.u면 CDN pv(영상/이미지 poster)로 카드 렌더. `findWork`/`buildGallery`/`renderPerf`가 W·userWork 공통 처리. userWork 카드 제목은 `works.add`가 단일 소스(인라인 편집 안 함).
  - 관리 모달 닫으면 `rerender()`(render+편집상태 복원)로 카드 반영.

## 최근 상태 (2026-07) — 피그마 기반 전면 리디자인
- **원본 Figma 학습.** `figma.com/design/8BgbjFxQTzFQQvBiHRRetW`(사용자 소유, 편집 공유로 접근). 스터디: 스크래치패드 `figma_study.md`. 핵심=지적 훅 '새로운 조합, 새로운 의미' + 수사학 3요소 + 라이트↔다크 시네마틱.
- **프레젠테이션 레이어 전면 교체(데이터·기능 계약 100% 보존).** `<style>`+섹션 렌더 함수만 교체, A/W/PKG/CATALOG·편집·라이트박스·큐레이션 JS는 그대로.
- **섹션 순서:** 히어로 선언(라이트) → 이력서 스트립(라이트) → TOP3 수사학(다크) → 프로세스(다크) → 아카이브(다크) → 성향(라이트) → 컨택트(라이트). 렌더 함수: heroHTML/resumeHTML/rhetoricHTML/processHTML/archiveHTML/aboutHTML/contactHTML.
- **히어로:** 두괄식 슬로건 '새로운 조합, 새로운 의미'(둘째 줄 `.grad` 마젠타). 3줄 매니페스토(verbatim, A.mani). 실집행 스탯밴드(A.stats, PKG 11개 실합산: ₩2.6억·8,575만·33만·8.76%)+MAU 2,200만. data-k: r.slogan0/1, r.mani0-2, r.desc, r.tag3_0-2, r.stat{i}v/l, r.mau, r.statcap.
- **TOP3(다크) = `const TOP3`.** 수사학 3요소: 시선 유도 Eye Trace(Spotify RE:SING=로컬 mg_charge) / 상징적 연결 Metaphor(잠이오질않네요=YT Z6hTS4C-4Ls) / 무의식 자극 Subliminal(Next to Zero=YT J4zBpmVj49c). 카드=툴태그+이중언어+연출의도+도발질문+메타표, 클릭→유튜브(`t3media` data-yt). ※이 TOP3는 개인/수상작. 당근 캠페인 작업은 아카이브(W).
- **컬러 토큰:** `--dark:#0d0d0f --cream:#f6f4ef --ink:#141317 --mut-c:#6b6660(크림 위 AA) --mut-d:#9a938a(다크 위) --mag1:#ff2f9c --mag2:#ff6ab0 --magink:#cc1f77(크림 위 강조) --grad:105deg`. 시그니처=A+B→의미 방정식(W[i].combo 캡션)+선형 마젠타. **마젠타 블롭 금지(AI 클리셰)**.
- **타입:** Pretendard 900 디스플레이 + mono(데이터/영문 메타). 영문은 한글의 메타로 강등(같은 크기 병렬 금지).
- **접근성/성능:** focus-visible(다크=크림·라이트=ink 링), `prefers-reduced-motion` 가드(오토플레이·리빌 정지, RMOTION 상수), aria-label/role, 영상 preload=none+poster+IO 1개 재생. 스크롤 리빌 `.rv`+`revO`.
- **info 키 주의:** content.json 옛 `r.info.2`=전화·`r.info.3`=naver 이메일과 충돌 피하려 이력서 생년/이메일은 신규 키 `r.birth`/`r.email` 사용. 직무는 `r.info.0`(override 유지). 컨택트 대형 헤딩은 `ui.ctBig`(옛 `ui.ctH`=Contact와 분리).
- **정적 검증:** node 하네스로 스크립트 파싱 + 7개 섹션 렌더 문자열 확인(브라우저 미사용). 적대자 3인 코드 감사 워크플로우로 반복 디벨롭.
- **⚠ 이전 '베스트3=td_review2605·mg_bizreview·td_declutter' 규칙 폐기.** 사용자 원본 TOP3(수사학 개인작)로 대체. 3 당근 정사각작은 아카이브에 잔류.

## 최근 상태 (2026-07) — 피그마 페이지 단위 '그대로 이식' (2차 보정, 최종)
- **사용자 2차 피드백 8개:** ①이력서(#1) 페이지를 양식·글자비율·프사 뒤 블롭까지 그대로(판떼기 이미지 금지) ②스탯밴드(₩2.6억/노출/CTR/MAU) 멋대로 넣지 마라—삭제 ③'새로운 조합'은 원래 콤보 카드(가짜 꽃 정원/AI 첫사랑/포토그래메트리) 페이지다—본문 살려서 한 페이지 그대로 ④MAIN 세로 무시 ⑤스타일 그리드는 개별 이미지로 (판떼기 금지) ⑥코딩/노드도 개별 이미지+원래 움짤 ⑦성향 섹션 삭제 ⑧포폴을 저급하게 만들지 마라.
- **피그마 정밀 추출:** get_design_context(291:5 이력서, 291:327 히어로, 291:263 코딩텍스트)로 정확한 타이포/컬러 확보: 이력서=Pretendard **ExtraLight 200** 지배(헤더 36px/본문 16.69px/행간 33.38px), 신상 라벨 18px rgba(0,0,0,.5), 레드박스 **#a61c2f**·보더 #ac1e25·태그라인 강조 **#cc1433**, 슬로건 ExtraBold 96px에 강조단어 **#7f0036+핑크텍스처(slog1/2.png) screen 블렌드**(웹은 background-clip:text로 재현), 콤보카드 775×242·타이틀 Bold 29px·스크림 transparent 60%→black 86%, C1/C2 타이틀 ExtraBold 40px·본문 ExtraLight 18/36·pill 흰 아웃라인 19.5px.
- **원본 자산 대량 이식 → `assets/fig/`:** photo.png(증명사진+HANGYEOL 스티커 합성), blob.png(핑크 블롭), ic_*.png(기술 아이콘 4), slog1/2.png, combo1-3.jpg(콤보 카드 사진), st_c1a~c3d(스타일 13타일=슬랩 좌표 슬라이스), cd_*(코딩 5타일), nd_*(노드 8타일), **sea.mp4**(물결 100프레임 GIF→h264)·**move.mp4**(AE 관성 데모 88프레임 GIF→h264, autoplay muted loop). GIF 원본은 download_assets rawImages로 확보(원본 포맷 유지됨). Figma MCP 요금제 한도로 노드 말풍선 내부 미니 이미지 6장만 미확보(텍스트 캡션으로 대체).
- **섹션 구성(피그마 페이지 순서 그대로):** ①resumeHTML=이력서 한판(블롭+신상카드+태그라인+챕터프리뷰 / 우측 학력·경력·기술·수상 6열 12행 전부 피그마 원문) ②heroHTML=새로운 조합(슬로건+매니페스토+콤보 3장 원문) ③c1HTML=생각의 유도(TOP3, 이미지 좌+텍스트 우 동일 방향, 메타 제목/구분/연출의도/줄거리/색상/제작기간 원문, 인라인 유튜브) ④c2HTML=기술의 개선(스타일 3열 masonry·코딩·노드 각각 개별 타일+무비, 말풍선 원문) ⑤archive(기존 갤러리+PKG 유지) ⑥footer 미니멀. 챕터 경계는 흰 보드(chapBd, 피그마 BD 재현). about/contact/스탯밴드/헤더 전부 삭제.
- **콘텐츠 규칙:** 피그마 원문이 기본값. 단 (a)기술사항은 기존 제거 지시(RealityCapture/Unreal/StableDiffusion/PPT/Notion/Obsidian) 반영한 그룹 포맷(2D-/3D-/AI-/PT-) (b)태그라인·소개문은 사용자가 고친 문구(r.tagA/r.tagB/r.desc) (c)주소·전화 행은 공개 리포 privacy로 기본 빈값(편집모드에서 채우면 노출, .pr.off 토글) (d)경력 종료일은 '현재'로. C103 연출의도가 피그마에서 C102와 중복(원본 파일 상태) → 해당 행은 제외하고 스토리 행 유지.
- **편집키 마이그레이션:** 이력서 신규 키(r.eduf/r.carf/r.awf/r.skg/r.tagA·B/r.pi.*, r.t3.*, r.c2.*, r.cc*). content.json에서 사어 키(구 r.edu/r.aw/r.sk/r.info/r.tag/ui.nav*/photo.*) 61개 일괄 삭제(saveEdits가 되살리는 것 방지). 사용자가 직접 고친 r.desc·w.* 오버라이드는 보존. photo.bs/bx/by·사진 드래그 편집기 제거(피그마 합성 프사 사용). 신상 메일은 피그마의 개인 gmail 대신 공개용 hansy@daangn.com(ui.email) 사용 — 의도된 교체.
- **적대자 감사 반영:** noscript 색상(흰 배경 대비), 콤보2 참여 문구 쉼표(원문 복원), C1 메타 후행 마침표(원문 복원), TOP3 동시재생 방지(go()에 stopT3+editing 가드), og:image 절대경로(top3_eyetrace), 편집 포커스 색 구 마젠타 잔재 제거.

## 최근 상태 (2026-07) — 3차: 정보 복원·디자인 시스템·편집 플랫폼 (14개 지시)
- **크리티컬 정보 복원(사용자 명시 승인):** 신상카드에 주소·전화 전부 기입, 메일=개인 gmail(hansy.dawn@gmail.com, content.json ui.email·푸터·noscript 전부). 사진은 이전 증명사진(assets/img/profile.jpg, bg 196%/43.1%/23.3% 크롭)으로 복귀. ⚠ **면접 연락용 메일은 회사메일 금지.**
- **기술 사항 영구 규칙:** "AI - Veo3, ComfyUI, PostShot, Topaz / PT - Figma" 줄 자체를 항상 제거. 2D·3D 두 줄만.
- **이력서 웹 보정:** 표를 열별 `<br>` 나열→행 단위 grid(.tgrid/.awgrid, 셀 줄바꿈에도 정렬 유지), 수상 6열 스크롤 없이(글자 축소, ≤640px는 날짜|분야/대회명/상·역할·주최 행 구조), 본문 200→300 가독성, 명함 ≤480px 세로 스택, --tlab 대비 4.5:1(0.56).
- **디자인 시스템(docs/디자인-시스템.md):** 콘텐츠 영역 그림자 전면 금지(명함 포함 — 피그마 원본에도 없음), 챕터 목차·챕터 헤더·lab 아이템이 같은 '빨간 라벨+볼드+얇은 부제' 그리드 반복, pill 크기 위계 정상화, FAB·푸터 페이지간 통일(--red2).
- **갤러리 통합:** 스타일 타일 13장(ST 배열, sea.mp4 포함)을 W 사이에 인터리브(gridWorks). 순수 갤러리뷰 — 캡션 제거, 호버 시 하단 좌측 제목+역할 오버레이(.hov), 터치는 라이트박스로 보완. 필터에 '스타일' 추가, 필터 상태 유지(FILTER).
- **C2 = 코딩·노드 2블록만** (스타일 블록은 갤러리로 이관).
- **편집 플랫폼:** ①활성화=키워드 `hansy` 타이핑(또는 #hansy 해시), #edit 폐기 ②모든 텍스트 data-k ③파일 업로드(25MB, 제목/설명/역할/분류/비율, 영상 썸네일 자동 첫프레임 추출 captureFrame) → assets/up/ ghPut ④작업 숨기기/보이기(works.hide) ⑤모달에서 제목·역할·설명 편집(bind, W는 EDITS+DOM 동기화, u는 works.add) ⑥저장 전 서버 재fetch 병합(freshMerge, 다중 탭 lost-update 방지) ⑦rerender 시 미저장 편집 스테이징.
- **lab.html 신설:** 도구 개발·워크플로우 연구 5항목(CreativeReporter/모션 템플릿 자동화/AI 파이프라인/논문/퍼블리싱 시스템), 자체 편집 엔진(l.* 키), 목차 3행에서 링크.
- **적대자 2회전 감사 반영:** 명함 주소 모바일 잘림, 편집중 카드 키보드/포인터 이벤트, 모달 편집 유실(스테이징+DOM 동기), 히어로 카드 모바일 오버레이 재구성(aspect 해제+relative), 편집 토글 재렌더, awgrid 모바일 그리드, captureFrame 누수/이중 업로드 가드 등 15+건.
- **문서:** docs/ = 옵시디언 볼트(00-인덱스/디자인-시스템/백로그/결정-로그). 새 작업 전 백로그 확인.
- **미해결(백로그):** 노드 말풍선 미니 이미지 6장·C102/103 필름스트립(Figma MCP 한도), 작업 순서 변경, 주소 비공개화(저장소 난수명 변경은 소유자만 가능).
- **사용자 핵심 불만:** 재해석 말고 피그마의 룩을 살리면서 구조를 충실히 이식할 것. 내가 만든 실제 콘텐츠(이미지/움짤)를 버리지 말 것. 형식용 요소·설명·선을 없애고 오직 '나에 대한 정보'만 남길 것.
- **피그마 실제 이미지 6종 이식** → `assets/fig/`: TOP3 스틸 `top3_eyetrace/metaphor/subliminal.png`(각 영상 썸네일=사용자가 넣은 사진), 챕터2 `style_grid.png`(다양한 스타일 시도 모자이크)·`coding.png`(AE 익스프레션+SEED 템플릿 UI)·`nodes.png`(C4D Xpresso·Python 노드 그래프). Figma `download_assets`로 export(202 폴링 다운로드).
- **1) TOP3 인라인 재생:** 유튜브 페이지 이동 폐기 → 클릭 시 컨테이너 안에 iframe(`youtube.com/embed/{yt}?autoplay=1`) 삽입해 그 자리에서 재생(`.t3media.playing`). 포스터=사용자 스틸(`TOP3[i].poster`). 라이트박스/편집 진입 시 `stopT3()`로 iframe 제거.
- **2) 수상 색상 표기 제거:** `.awrow.top` CSS·등급별 `.top` 토글 JS 삭제. 전 수상 동일 스타일.
- **3·7) 헤더 삭제 + 서론 압축(한판):** sticky `header.top`(브랜드/nav) 전면 제거. `heroHTML`+`resumeHTML` → 단일 `introHTML()`(이력서 보드: 사진+이름/직무+태그라인(r.tag)+설명(r.desc)+생년/이메일+역량/기술 + 경력/학력/기술 3열 + 수상 + 스탯밴드). 첫 화면=압축 이력서.
- **4·5) 챕터2 실이미지 복원:** `processHTML`(텍스트 3스텝) 폐기 → `techHTML()` = 다양한 스타일 시도/코딩 작업시간 단축/노드 체계화 3블록, 각 `A.tech[i]`=[번호,제목,설명,이미지경로,alt]. 실제 화면·움짤 이미지를 바로 보여줌.
- **8) 불필요 요소 제거:** `.edge` 마젠타 장식선, 섹션 `<h2><em>` 영문 부제, 운영 설명 노트('스크롤 시 자동재생·클릭 시 성과' 등), TOP3 요약 nav(`.t3sum`), 아카이브 카드의 생성형 `combo` 캡션(`.cmb`) 모두 삭제.
- **섹션 순서(현재):** intro(이력서·라이트) → rhetoric(선언 슬로건+매니페스토+TOP3·다크) → tech(기술의 개선·다크) → archive(작업·다크) → about(성향·라이트) → contact(라이트). 렌더 함수: introHTML/rhetoricHTML/techHTML/archiveHTML/aboutHTML/contactHTML.
- **보존:** content.json 편집(data-k)·CreativeReporter 실성과(PKG)·라이트박스·소재 큐레이션 JS 전부 그대로. 정적 검증: node 하네스로 파싱 + 섹션 렌더 문자열/태그밸런스 확인(브라우저 미사용).

## 최근 상태 (2026-07) — 4차: 챕터 바·크기 조절 시스템·도구/연구 섹션
- **스크롤 챕터 바(#chapbar):** 반투명 블러 고정 바, 스크롤 위치 따라 개요(인적 사항…)/주요 작품/작업 효율화/작업 아카이브/도구·연구 표시. 목차(chprev 4행)·챕터 헤더(chaphd)와 동일 워딩. 라이트박스 열리면 숨김.
- **크기 조절 시스템:** 레이아웃 비율은 CSS 변수(--rc/--hc/--c1c/--c2c = "Xfr 1fr", --agap px), 편집 모드에서 컬럼 경계 빨간 핸들(.szh) 드래그로 조절, 갤러리는 간격 슬라이더(#gapRange). 값은 EDITS sz.* → content.json 영구 저장, applySizes()가 :root에 적용. 각 컬럼은 container-type:inline-size + 폰트 cqw라 너비 변경 시 글자가 비율대로 스케일(ctrl+t 느낌). 모바일 미디어쿼리는 !important로 1열 강제.
- **사진 크롭 편집 복원:** .idphoto 배경(기본 160%/43%/20%), 편집 모드 드래그=위치·휠=확대, photo.bs/bx/by 저장.
- **도구·연구(#lab) 본문 섹션화(lab.html 삭제):** CreativeReporter(실물 대시보드 스크린샷 assets/lab/cr_shot.jpg — 헤드리스 크로미엄+RPC 인터셉트로 촬영), AI 이미지 소재 파이프라인(노션 '이미지소재 자동화툴 제작기'에서 구조도 ai_arch.png·플러그인 실물 ai_tool.png 이식; 사실관계: 자체 피그마 플러그인+Node.js 브릿지 서버, OAuth로 생성모델 MCP 직접 호출, 1장당 약 300원=시중 1/5, 팀 배포용 설치파일), 모션 템플릿 자동화, 논문. '이 사이트' 항목은 사용자 지시로 제외. l2.* data-k.
- **C1/C2:** 카드 상하 간격 확대(clamp 56~140px), 기본 비율 2:1/2.2:1(조절 가능).

## 최근 상태 (2026-07) — 5차: 콘텐츠 보호·히어로 재디자인·이미지 타일 비활성
- **콘텐츠 보호(편집 모드 제외):** body user-select:none·touch-callout 차단, contextmenu/dragstart/copy 차단, Ctrl(⌘)+S/U/P/C·Shift+I/J/C/S 차단, PrintScreen keyup 시 클립보드 클리어, @media print 숨김, 라이트박스 video controlslist=nodownload·PiP 금지. OS 스크린샷은 웹에서 원천 차단 불가(사용자에게 고지).
- **히어로 콤보 카드 재디자인(사용자 재량 위임):** 이미지 위 흰 글자 오버레이(스크림) 전면 폐기 → `이미지(16:10, 12px 라운드) + 텍스트` 플랫 행 3개, 위아래 헤어라인. 제목 Bold/질문 Light/작품·참여 라벨 — 목차·챕터와 같은 구조 언어. 텍스트는 hcards 컨테이너 cqw 스케일.
- **이미지 단독 작업물 비활성(imgOnly):** st 타일(영상 없는 12장)과 이미지-only 업로드는 role/tabindex/hov/클릭 모두 제거(.noact). 편집 모드에선 정상 관리(숨김·순서·제목). 물결(sea.mp4) 타일은 영상이라 활성 유지.

## 최근 상태 (2026-07) — 6차: 히어로 폐기·도구/연구 개편·스티키 챕터바 일원화
- **히어로('새로운 조합') 섹션 삭제**(사용자: 낭비). 슬로건·매니페스토·slog 텍스처 제거. 콤보 카드 3장(전시/영화/논문)은 도구·연구 우측 '실험 · 연구' 컬럼으로 이동(expCardsHTML).
- **도구·연구(#lab) = 흰 배경 2컬럼**(--lc 조절 가능): 좌 '도구 개발'(CR 실물 스크린샷 / AI 파이프라인 구조도만 — 플러그인 실물 화면 제거 / 모션 템플릿), 우 '실험 · 연구'(콤보 카드). 큐레이션 문장: "제작의 반복을 코드로 줄인다…" / "이질적인 것을 조합해 새로운 의미를 검증한 개인 프로젝트…". 논문 별도 항목은 콤보3과 중복이라 제거.
- **본문 챕터 헤더(chaphd) 전면 제거** → 스티키 챕터바(#chapbar)로 일원화: 글씨 14-16px, 다크 섹션(c1/c2/archive) 위에선 rgba(16,16,19,.6) 다크 블러로 전환(.dk), 라이트에선 흰 블러. 스크롤 140px 이후 표시.
- **주요 작품(C1)에 당근 대표작 확장(C1X=["td_declutter"])**: 16:9 프레임(정사각 원본 커버 크롭), 클릭→라이트박스(성과 포함), 메타=구분/연도/성과(실집행). 가로형 후보: keyword_spring·raffle·jobs_spring(CDN 1920×1080), 3d_sew(로컬 16:9).
- applyEdits가 같은 data-k 다중 요소 전부 갱신(querySelectorAll+CSS.escape). SIZERS: hc 제거, lc 추가.

## 최근 상태 (2026-07) — 7차: 타이포 고정 스케일·핸들 싱크
- **⚠ 보안 규칙: GitHub PAT를 저장소(index.html 포함)에 절대 하드코딩하지 말 것.** 공개 repo라 즉시 탈취/GitHub 자동폐기됨. 사용자가 채팅으로 PAT를 준 적 있음 → 코드에 넣지 않았고, 대신 키워드(hansy) 입력 즉시 getPAT() 1회 프롬프트 → localStorage 영구 기억으로 해결. (사용자에게 해당 토큰 회전 권고함)
- **타이포 정책 변경(사용자 확정):** 너비 조절은 레이아웃만 — 글자·이미지 크기 불변. cqw 전면 제거, 고정 클램프 스케일로 복귀(디자인-시스템.md 표 갱신). 큰 얇은 헤더는 22–32px로 상한.
- **C1·C2 비율 핸들 싱크:** SIZERS에서 .t3card와 .c2blk가 같은 키 sz.cc 공유(기본 2fr 1fr). rleft 상단 여백 제거로 좌우 상단 정렬.

## 최근 상태 (2026-07) — 8차: 태그라인 단일화·카탈로그 일괄 그룹 추가
- **태그라인:** r.tagA/r.tagB 2-span 구조가 saveEdits의 후행공백 제거로 '투자하는김한결' 붙는 버그 유발 → 단일 키 r.tag2, 단일 색(검정)으로 교체. content.json 마이그레이션 완료.
- **카탈로그 일괄 추가:** creatives.json에서 maugrowth/vidmaugrowth 계열 중 기존 작업과 무관한 30패키지를 '해상도 토큰 제거 후 동명 병합' 규칙으로 23개 그룹 작업 생성 → works.add. 대표=세로 영상 우선(computePV 동일 로직), perf는 변형 합산(ctr는 노출가중, 20만 미만 표본 null). id=cr_<base>_<md5 4자리>(한글 슬러그 충돌 방지). 제목은 편집모드에서 다듬으면 됨.
- **declutter 가로 자동 훅:** c1xCard(td_declutter)가 업로드 작업 중 1920x1080 영상+제목/캠페인에 '짐|declutter' 매칭되는 항목을 찾으면 그 영상으로 자동 교체(자동재생, 클릭 시 해당 작업 상세). 로컬 원본은 사용자만 갖고 있어 업로드 대기.
- **미집행 로컬 작업물 한계:** daangnVideo/daangnIMG 폴더는 사용자 PC 경로라 접근 불가. CreativeReporter에 없는(집행 안 된) 작업은 카탈로그로도 파악 불가 → 업로드로만 추가 가능.

## 최근 상태 (2026-07) — 9차: 자동 추가 롤백·고정 데스크탑 뷰
- **⛔ 규칙(사용자 강한 지시): 작업물을 임의로 일괄 추가하지 말 것.** 카탈로그 자동 추가 23개는 본인이 만들지 않은 것이 섞여 전량 롤백(works.add=[]). 작업 추가는 오직 사용자가 편집모드 카탈로그 선택으로. CR 데이터에 제작자 필드는 없음(ad_name/channel/payload 지표뿐) — '제작자 태그' 역할은 사이트의 카탈로그 선택 기능이 담당.
- **고정 데스크탑 뷰(사용자 확정):** `<meta name="viewport" content="width=1440">` + 레이아웃 붕괴 미디어쿼리(1열 전환·스택·컬럼 축소) 전부 삭제. 모바일은 데스크탑 화면을 통째로 축소해 보게 됨. 반응형 재배치 재도입 금지. (라이트박스/관리 모달의 UI 쿼리만 잔존 — 모바일에선 발동 안 함)
- declutter 가로(1920x1080)는 CDN에도 없음(CR 성과만 있고 미디어 없음) → 업로드가 유일한 경로, 자동 감지 훅은 유지.
